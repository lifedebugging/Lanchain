Previous model config handling

Module level "model_configs" connected to llm endpoints inside "lifespan" via global variables


#model configuration
MODEL_CONFIGS = {
    "Fast": {
        "model": "llama-3.3-70b-versatile",
        "temperature": 0,
    },
    
    "Smart": {
        "model": "openai/gpt-oss-120b",
        "temperature": 0.7,
    }
}



#LLM endpoints
gpt = ChatOpenAI(
    model=MODEL_CONFIGS["Smart"]["model"],
    api_key=os.getenv("GROQ_API_KEY"),
    base_url="https://api.groq.com/openai/v1",
    temperature=MODEL_CONFIGS["Smart"]["temperature"],
    )

llama = ChatOpenAI(
    model=MODEL_CONFIGS["Fast"]["model"],
    api_key=os.getenv("GROQ_API_KEY"),
    base_url="https://api.groq.com/openai/v1",
    temperature=MODEL_CONFIGS["Fast"]["temperature"],
    )



if decision["model"] == "Fast":
        …

what's missing?
retry logic
no fallback llm
weak decision relying on string



Current Model config handling and model routing handling

class ModelTier(str, Enum):
    fast = "Fast"
    fallback = "Fallback"
    smart = "Smart"
    
    
@dataclass
class ModelConfig:
    model_name: str
    temperature: float
    max_tokens: int = 4096
    
MODEL_MAP = {
    ModelTier.fast: ModelConfig(
        model_name= "llama-3.3-70b-versatile",
        temperature= 0,
        max_tokens=4096
        
    ),
    ModelTier.smart: ModelConfig(
        model_name= "openai/gpt-oss-120b",
        temperature= 0.8,
        max_tokens=4096
    )
}

right now model endpoins and agents are being created two times inside lifespan. changing it to create dynamic endpoints and agents.



LOGs:

My plan was to remove the "if/else" and relying on string to switch models eg: "model== "fast"/"slow".

The process of streaming and invoking was similar in both fast and smart model hence I thought why not create dynamic model switch.

Hence I create `model_config.py`

To achieve typo-proof switch I came across `Enum`

`Enum` let's you define named constants with values, so instead of comparing  decision["model"] == "Fast" you'd compare decision["model"] == ModelType.FAST. Typo-proof, IDE autocomplete, no strings.


Basic syntax of Enum

1. `from Enum import Enum`
2. define a class and pass the `Enum` as parent class

   class ModelTier(Enum):
       color = 1
       world = 2

  THis creates a key/value pairs.
 
  Access them by: attribute access

3. then to make it dynamic I realized I have to store the Config to pass to agent and endpoints.

    @dataclass
    class ModelConfig:
    model_name: str
    temperature: float
    max_tokens: int = 4096
 
   here I created another class ModelConfig simply to store values given  by @dataclass

4. Now to pass two different model names and configuration for each "fast" and "smart" model.
   
   I have to create a dictionary

  MODEL_MAP = {
    ModelTier.fast: ModelConfig(
        model_name= "llama-3.3-70b-versatile",
        temperature= 0,
        max_tokens=4096
        
    ),
    ModelTier.smart: ModelConfig(
        model_name= "openai/gpt-oss-120b",
        temperature= 0.8,
        max_tokens=4096
    )
}

   I have done this before in routing

     inside the dictionary holds two key/value pair.
     `ModelTier.fast` which is how we access class variables basically these is similar to writing
      `"Fast" : ModelConfig()`

     the value is `ModelConfig`() we are writing values for each there are three requires we defined inside `ModelConfig` class

     similar for `ModelTier.smart` which is similar to "Smart" = some value.


5. next is `log_routing_decision` function.

  it takes decision which is `dict` type in `decision logic` and return "ModelConfig"

  It has 4 values to log and print, `category`, `confidence`, `model_tier_str`, `tools`
  
  each category has default value to prevent printing empty value and using `.get` dictionary method to access specified key from a dictionary 
  this also to prevent "raising a keyError" when key doesn't exist.

  Inside it is try/catch block where it raises `ValueError` when tier = ("fast, "smart") is not one of them,
  personally I thought this wasn't needed because we are using `Enum` for "Typo-proof" switch but I added anyway.

  `config = Model_map[tier]` is basically config = "Fast" and what values assigned to "Fast" i.e. ModelTier.fast inside model_map can be accessed by    
   config now.
   
  Rest is just printing bunch of logs.

6. Now Upon this I didn't wanted to complicate it any further because this alone took me quite a while.

  so I started Importing all these to main file and replacing if/else model decision block.

  While I was in the middle of it stuck to define how am I gonna bind the decision and use it invoke different agent, so I created a agent lookup.
  
  It was simple lookup `{ModelTier.fast: fast_agent, ModelTier.smart: smart_agent}`

  However ModelTier.fast is a dictionary and you cannot perform `.astream` on it.

 That's when I realized I should clean up the endpoints and agent creation as well because well I was creating endpoints and agent both two times   inside `lifespan`.
  
7. "endpoint" function,

  Now as easy it sounds I was making lot's of syntax error for first time.

  Endpoint function was simple it takes `config` and return the endpoint inside the function.

  since config can access `Model_MaP` key/values I assigned `model = config.model_name` i.e when 
  
  config = "Fast", it is accessing whatever assigned to `ModelTier.fast` inside `Model_map` there are three values model_map, temperature and max_token.

  so when I do `config.model_name` it is accessing the name from Model_map assigned to that specific tier at that time similar for temperature

  `config.temperature` the resit of api key and base url is unchanged only model names and temeperature changes.
  
  this returns llm the endpoint gets created.

8. Inside main file I import the endpoint as well.

    ```
    agent_lookup = {}
    for tier, config in MODEL_MAP.items():
        model_endpoint = endpoint(config)
        agent = create_agent(
            model_endpoint,
            tools,
            checkpointer,
            system_prompt="check"
        )
        agent_lookup[tier] = agent
   ```

   This is new agent lookup I created,  where I loop for both tier "fast" and "smart".

   store it's tier which will be ModelTier.fat and config which is the three value assigned as ModelConfig(..)

   remember the function `def endpoint(config)`? 

   this is where we pass the `config` we call the function as `model_endpoint` and pass the ` config`   = `endpoint(config)`
  
   and create agent inside here single agent.

   `create_agent` requires model endpoint to create agent and that's what we were `returning` inside `endpoint` function.

   so we pass:

   ```
     model_endpoint,
            tools,
            checkpointer,
            system_prompt="check"
        )
   ```

  and we store the agent inside agent_lookup which is an empty dictionary {} for now.

  when tier will be assigned "Fast"/ "Smart"

  it will store "Fast" = agent

  which is agent which get created 

  ` agent_lookup[tier] = agent`

  This completes our dynamic agent inside `lifespan` and since `endpoint` function is created at module level inside `model_config.py` it will run only once at startup

9.  Inside `main` function

    I called `log_routing_decision` function in a variable `tier`.

    now `Config` gives you model settings. But you need tier to look up the agent. What should you return?
 
    simplest fix is return `tier` from log_routing_decision`
    
    remember the one inside try/catch block? `tier = ModelTier(model_tier_str)`

    so, inside main function: `agent_call = agent_lookup[tier]`

    this will get me something like "agent_call = ModelTier.fast/ .smart"

    and agent_call will be passed to .astream_events inside the `async generator`
  
 
10. So the whole picture would be:

    agent_call "stores" agentTier.fast or .smart from `agent_lookup`

    `agent_lookup` itself is a dictionary which stores `agent` created to each `tier`
   
    so when `agent_call` will be passed to `.asteam_events` it will lokup and to what is it is `ModelTier.fast` then the agent object itself created for "fast" model will be stored to `ModelTier.fast` key  

    the value assigned to `ModelTier.fast` which is agent itself will be passed to `.astream_events` since agent only can invoke the .astream_events
   it will use the value of specified tier to invoke and stream the result.

 

  

 
  